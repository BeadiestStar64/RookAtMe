# 0. はじめに

Rook At Meのコントリビューティングに興味を持っていただき、ありがとうございます。
皆さんのお時間を無駄にしないためにも、Rook At Meの簡潔なコントリビューティングポリシーをまとめました。

## ✅ PRが満たすべき条件

Pull Requestsが受け入れられるためには、以下の項目を全て満たしているか確認してください。

1. このPRに含まれるコードの作成に、生成AIを使用していません。または、生成AIを使用した場合、その使用範囲は [VisiDataが提案している **レベル1**](https://www.visidata.org/blog/2026/ai/) 以下です。
2. このPRに含まれるコードについてメンテナーから説明を求められた場合、最初に説明を求められた日から14日以内に生成AIの回答に依存せずに実装の意図、動作、採用した手法の理由を説明できます。
3. 外部の仕様、API、アルゴリズム等を根拠として実装した場合、メンテナーから求められた際に、その根拠となった仕様書、公式ドキュメント、ソースコード、Webサイト等の参考情報を提出できます。
4. マージ前に、必須のステータスチェックに全て合格しています。
5. このPRに含まれるコード、データ、その他の著作物について、Rook At Meが採用しているライセンスと互換性があります。

> [!NOTE]
> メンテナーから説明を求められた後、最初に説明を求められた日から14日以内に回答がない場合はPRをクローズする事があります。

## ❌ 受け入れられないPR

以下のどれか1つに該当する場合、残念ながらコード品質、説明可能性、及びライセンス適合性を担保するために、PRを受け入れる事ができません。

- このPRには、 [VisiDataが提案している **レベル2**](https://www.visidata.org/blog/2026/ai/) 以上に該当するAI利用が含まれています。
- このPRに含まれるコードについてメンテナーから説明を求められた場合、最初に説明を求められた日から14日以内に、生成AIの回答に依存せずに実装の意図、動作、採用した手法の理由を説明できません。
- 外部の仕様、API、アルゴリズム等を根拠として実装しているにもかかわらず、メンテナーから求められた場合に、その根拠となった仕様書、公式ドキュメント、ソースコード、Webサイト等の参考情報を提出できません。
- マージ前に、必須のステータスチェックに全て合格していません。
- このPRに含まれるコード、データ、その他の著作物について、Rook At Meが採用しているライセンスと互換性がありません。または、ライセンスを把握していません。

# 1. AIポリシー

> [!WARNING]
> Rook At Meでは、生成AIによって生成または補完されたコードを含むPRを受け入れていません。
> コードの作成に生成AIを使用する場合は、 [VisiData AI Level 1](https://www.visidata.org/blog/2026/ai/) 以下に留めてください。

Rook At Meは信頼性と堅牢性、説明可能性を重視しています。

コードを作成する時に、ChatGPT等のAIアシスタントを調査、議論、コードレビュー、デバッグ、実装方法の検討などの補助として利用する事は歓迎致します。
しかし、生成AIが生成したコードまたは補完したコードをRook At Meへ取り込む事は、コード品質や説明可能性の観点から認めていません。

Rook At Meでは生成AIとの会話そのものは禁止していません。
しかし、その会話中に生成されたコードをコピー、軽微に改変、または転記してPRへ取り込む事は認めていません。
生成AIから得たコードそのものではなく、生成AIを活用して調査、議論を通して得た知識を基に、コントリビューター自身が実装する必要があります。

GitHub Copilot等による生成AIベースのコード補完や、Codex、Claude Code等にソースコードを直接生成、編集させた内容をPRへ取り込む事も認めていません。

# 2. Rook At Meのポリシー

Rook At Meでは、コーディング熟練者もコーディング初心者も歓迎いたします。

> [!TIP]
> Rook At Meで不明点やコーディングの質問があれば、Pull Requestsではなく Discussionsをご利用ください

PRでは、日本語または英語をご利用ください。DeepLやGoogle翻訳といった翻訳サービスの利用は、歓迎されます。

# 3. 開発環境のセットアップ方法

このプロジェクトでは、以下の環境が必要です。

- Clang: 21
- Rust toolchain: Stable
- Rust Edition: 2024

# 4. ブランチの命名規則

PRのブランチは、以下の命名規則に従う必要があります。

```
<prefix>/<summary>
```

prefix名は、以下が使用できます。

| prefix名 |         ブランチの内容         |
| :------: | :----------------------------: |
|   feat   |           新機能開発           |
|   fix    |            バグ修正            |
|  hotfix  |         重大な緊急修正         |
| refactor |        リファクタリング        |
|  style   |     コードのスタイルの変更     |
|   perf   |       パフォーマンス改善       |
|  chore   |           軽微な修正           |
|    ci    |          CI/CDの変更           |
|  build   | ビルドシステム、依存関係の更新 |
|   test   |       テストの追加、修正       |
|   docs   |        ドキュメント変更        |
| example  |      exampleコードの変更       |

`summary` は、ブランチの内容を簡潔に表した英語で記述してください。この時、ケバブケースで記入してください。

破壊的変更であるかどうかはブランチ名には含めず、PRタイトルおよびコミットメッセージで明示してください。

# 5. コミットメッセージの規約

コミットメッセージは、以下の書式に従う必要があります。

```
<type>(optional scope)[!]: <short-description>

<optional body>
```

`type` には、以下を使用できます。

|   種類   |         コミットの内容         |
| :------: | :----------------------------: |
|   feat   |           新機能開発           |
|   fix    |            バグ修正            |
|  hotfix  |         重大な緊急修正         |
| refactor |        リファクタリング        |
|  style   |     コードのスタイルの変更     |
|   perf   |       パフォーマンス改善       |
|  chore   |           軽微な修正           |
|    ci    |          CI/CDの変更           |
|  build   | ビルドシステム、依存関係の更新 |
|   test   |       テストの追加、修正       |
|   docs   |        ドキュメント変更        |
| example  |      exampleコードの変更       |

`optional scope` はコミットの影響範囲を明示します。

破壊的変更を含む場合は、 `!` を `type` または `optional scope` の直後、 `:` の前に付ける必要があります。

`short-description` は、コミット内容を表す簡潔な英語または日本語で記述してください。

`optional body` は、コミット内容を具体的に記述します。コミット内容が `short-description` で十分に説明できる場合は、 `optional body` を省略しても構いません。
`optional body` は、日本語または英語で書く必要があります。

# 6. PRメッセージの規約

PRのタイトルは、以下の書式に従う必要があります。

```
<type>(optional scope)[!]: <short-description>
```

`type` は、 [**5. コミットメッセージの規約**](#5-コミットメッセージの規約) で定義されているtypeを使用してください。
`optional scope` はコミットの影響範囲を明示します。

破壊的変更を含む場合は、 `!` を `type` または `optional scope` の直後、 `:` の前に付ける必要があります。

`short-description` は、PR内容を表す簡潔な英語または日本語で記述してください。

PRの本文には、変更内容や変更理由など、PRの内容を具体的に記述してください。
PRの本文は日本語または英語で記述してください。
