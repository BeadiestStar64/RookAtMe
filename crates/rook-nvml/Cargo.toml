[package]
name = "rook-nvml"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
anyhow = { workspace = true }

[features]
default = ["native-nvml"]
native-nvml = []

[build-dependencies]
cc = { workspace = true }
